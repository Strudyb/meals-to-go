import { View, Text } from 'react-native';
import { SafeArea } from '../components/base';

const MapScreen = () => {
  return (
    <SafeArea>
      <Text>MapScreen</Text>
    </SafeArea>
  );
};

export default MapScreen;
